// ---------- راه‌اندازی نهایی (باید آخرین فایل لود‌شده باشد) ----------
load();
startAutoRefresh();
updateLiveClock();
setInterval(updateLiveClock, 1000);
startAlertChecking();
startAccountStatusRefresh();
